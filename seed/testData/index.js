//creates an object of data paths so they can be deconstructed in seed

module.exports = {
    articles: require('./articles'),
    comments: require('./comments'),
    topics: require('./topics'),
    users: require('./users')
};
